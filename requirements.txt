# ============================================================
# RAG 智能文档问答系统 — 依赖清单
# 安装: pip install -r requirements.txt --break-system-packages
# ============================================================

# LLM & LangChain 核心
langchain-openai>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# 向量数据库
chromadb>=0.5.0

# 嵌入 & 重排序
sentence-transformers>=3.0.0

# Web 前端
flask>=3.0.0

# 环境变量
python-dotenv>=1.0.0

# 文档加载器依赖
pymupdf>=1.24.0
unstructured>=0.15.0

# 数值计算（chromadb / sentence-transformers 依赖）
numpy>=1.26.0
